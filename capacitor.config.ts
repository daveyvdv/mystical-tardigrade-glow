import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.mysticaltardigradeglow',
  appName: 'mystical-tardigrade-glow',
  webDir: 'dist'
};

export default config;
